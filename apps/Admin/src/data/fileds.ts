const User: { [k: string]: string } = {
  User: 'id email verfied suspended username google apple createdAt',
  User_list: 'id email verfied suspended username google apple createdAt',
  update_User: 'suspended'
}

const Fileds: { [k: string]: string } = {
  ...User
}

export default Fileds
